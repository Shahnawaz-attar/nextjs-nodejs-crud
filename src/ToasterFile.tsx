import React from 'react';
import toast, { Toaster } from 'react-hot-toast';

const ToasterFile = () => {
  return <Toaster position="bottom-center" reverseOrder={false} />;
};

export default ToasterFile;
